// Unused in this project — kept as a placeholder so generated server templates compile.
// All auth flows are client-side via the Supabase JS client.
export const requireSupabaseAuth = undefined as unknown as never;
